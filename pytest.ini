[pytest]
asyncio_mode = auto
pythonpath = .
markers =
    slow: slow and/or network-using tests (the wheel packaging smoke test);
    excluded from the default run. Run explicitly with `pytest -m slow`
    (an explicit -m overrides the default below).
addopts = -m "not slow"
