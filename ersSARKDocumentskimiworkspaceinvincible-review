warning: in the working copy of 'tests/test_oauth.py', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'tests/test_tool_executor.py', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/.env.example b/.env.example[m
[1mindex 8ec4d1c..2c0f0e3 100644[m
[1m--- a/.env.example[m
[1m+++ b/.env.example[m
[36m@@ -4,4 +4,5 @@[m [mINVINCIBLE_OWNER_SECRET=    # one-time browser login for /oauth/authorize: authe[m
 NVIDIA_API_KEY=             # NVIDIA NIM hosted (tier 1): z-ai/glm-5.2[m
 GROQ_API_KEY=               # Groq (tier 2): openai/gpt-oss-120b[m
 OPENROUTER_API_KEY=         # OpenRouter fallback (tier 3)[m
[31m-GEMINI_API_KEY=             # Gemini fallback (tier 4, last resort)[m
\ No newline at end of file[m
[32m+[m[32mGEMINI_API_KEY=             # Gemini fallback (tier 4, last resort)[m
[32m+[m[32m# INVINCIBLE_READ_ROOTS=    # optional: extra directories read_file may access, separated by ';' (Windows) or ':' (Unix). The repo root and the server's working directory are always allowed.[m
\ No newline at end of file[m
[1mdiff --git a/invincible/core/tool_executor.py b/invincible/core/tool_executor.py[m
[1mindex bf4643a..c6be6ea 100644[m
[1m--- a/invincible/core/tool_executor.py[m
[1m+++ b/invincible/core/tool_executor.py[m
[36m@@ -36,6 +36,11 @@[m [mSecurity model - decided explicitly up front, not bolted on after the fact:[m
      independent of GATEWAY_API_KEY). This module assumes the caller is[m
      already authenticated - it only decides whether a specific action is[m
      safe and approved, not who's allowed to ask.[m
[32m+[m[32m  6. read_file has no approval step, so it is sandboxed instead: reads are[m
[32m+[m[32m     only allowed under the repo root, the server's working directory, and[m
[32m+[m[32m     any directories listed in INVINCIBLE_READ_ROOTS (os.pathsep-separated).[m
[32m+[m[32m     Paths outside those roots are blocked outright, and .env / sessions.db[m
[32m+[m[32m     / .git are blocked by name anywhere inside them.[m
 [m
 KNOWN LIMIT: the denylist is a text-pattern match, not a real shell parser.[m
 `powershell -Command "..."`, `cmd /c "..."`, or any other wrapper/encoding[m
[36m@@ -215,7 +220,45 @@[m [mdef _check_protected_path(path: str, patterns: list, verb: str) -> None:[m
             raise ToolBlocked(f"{verb} of {reason} ({rel})")[m
 [m
 [m
[32m+[m[32m# read_file is sandboxed to a small set of roots: the repo root, the[m
[32m+[m[32m# process working directory (where `invincible start` was run), and any[m
[32m+[m[32m# extra directories listed in INVINCIBLE_READ_ROOTS (os.pathsep-separated).[m
[32m+[m[32m# Anything outside those roots is blocked outright - no approval step -[m
[32m+[m[32m# because read_file has no approval step to act as the gate. Inside the[m
[32m+[m[32m# roots, the basename rules below block the files most likely to hold[m
[32m+[m[32m# actual credentials, wherever in the tree they sit.[m
[32m+[m[32m_BASENAME_READ_DENYLIST = [[m
[32m+[m[32m    (re.compile(r"^\.env(\..+)?$", re.I), "an .env file"),[m
[32m+[m[32m    (re.compile(r"^sessions\.db$", re.I), "the session database"),[m
[32m+[m[32m    (re.compile(r"^\.git$", re.I), "git internals"),[m
[32m+[m[32m][m
[32m+[m
[32m+[m
[32m+[m[32mdef _allowed_read_roots() -> list:[m
[32m+[m[32m    roots = [_REPO_ROOT, os.getcwd()][m
[32m+[m[32m    extra = os.getenv("INVINCIBLE_READ_ROOTS", "")[m
[32m+[m[32m    for entry in extra.split(os.pathsep):[m
[32m+[m[32m        entry = entry.strip()[m
[32m+[m[32m        if entry:[m
[32m+[m[32m            roots.append(entry)[m
[32m+[m[32m    return [[m
[32m+[m[32m        os.path.normcase(os.path.abspath(root)) for root in roots[m
[32m+[m[32m    ][m
[32m+[m
[32m+[m
 def check_read_denylist(path: str) -> None:[m
[32m+[m[32m    abs_path = os.path.abspath(path)[m
[32m+[m[32m    norm = os.path.normcase(abs_path)[m
[32m+[m[32m    roots = _allowed_read_roots()[m
[32m+[m[32m    if not any(norm == root or norm.startswith(root + os.sep) for root in roots):[m
[32m+[m[32m        raise ToolBlocked([m
[32m+[m[32m            f"read of path outside the allowed roots ({abs_path}). "[m
[32m+[m[32m            "Set INVINCIBLE_READ_ROOTS to grant access to other directories."[m
[32m+[m[32m        )[m
[32m+[m[32m    for part in abs_path.split(os.sep):[m
[32m+[m[32m        for pattern, reason in _BASENAME_READ_DENYLIST:[m
[32m+[m[32m            if pattern.match(part):[m
[32m+[m[32m                raise ToolBlocked(f"read of {reason} ({abs_path})")[m
     _check_protected_path(path, READ_DENYLIST_PATTERNS, "read")[m
 [m
 [m
[36m@@ -362,10 +405,12 @@[m [masync def confirm_action([m
 [m
 async def read_file(path: str) -> dict:[m
     """No approval step - reading isn't destructive, so the friction[m
[31m-    wouldn't buy anything. The denylist is the only gate: it blocks reading[m
[31m-    out actual secrets/state (.env, sessions.db, .git/) but deliberately[m
[31m-    allows reading invincible/ and tests/ and providers.yaml, since letting the[m
[31m-    cloud AI see the code is the entire point of this tool."""[m
[32m+[m[32m    wouldn't buy anything. The sandbox is the gate instead: reads are only[m
[32m+[m[32m    allowed under the repo root, the server's working directory, and any[m
[32m+[m[32m    INVINCIBLE_READ_ROOTS directories, with .env / sessions.db / .git[m
[32m+[m[32m    blocked by name anywhere inside them. Reading invincible/ and tests/[m
[32m+[m[32m    and providers.yaml stays allowed, since letting the cloud AI see the[m
[32m+[m[32m    code is the entire point of this tool."""[m
     check_read_denylist(path)  # raises ToolBlocked; caller maps it to a response[m
 [m
     try:[m
[1mdiff --git a/invincible/endpoints/mcp.py b/invincible/endpoints/mcp.py[m
[1mindex 9ea8ae8..28e96c0 100644[m
[1m--- a/invincible/endpoints/mcp.py[m
[1m+++ b/invincible/endpoints/mcp.py[m
[36m@@ -33,9 +33,11 @@[m [mTOOLS = [[m
     {[m
         "name": "read_file",[m
         "description": ([m
[31m-            "Read a file's contents from the host machine. Reads of files "[m
[31m-            "that contain secrets or sensitive state (.env, sessions.db, "[m
[31m-            ".git/) are rejected outright. No confirmation is required for "[m
[32m+[m[32m            "Read a file's contents from the host machine. Reads are "[m
[32m+[m[32m            "sandboxed to the server's working directory and repo root "[m
[32m+[m[32m            "(extend with INVINCIBLE_READ_ROOTS); files holding secrets or "[m
[32m+[m[32m            "sensitive state (.env, sessions.db, .git/) are rejected "[m
[32m+[m[32m            "outright wherever they sit. No confirmation is required for "[m
             "other files since reading is non-destructive."[m
         ),[m
         "inputSchema": {[m
[1mdiff --git a/invincible/endpoints/oauth.py b/invincible/endpoints/oauth.py[m
[1mindex 83ccdb7..0cbb3da 100644[m
[1m--- a/invincible/endpoints/oauth.py[m
[1m+++ b/invincible/endpoints/oauth.py[m
[36m@@ -146,6 +146,11 @@[m [mdef _base_url(request: Request) -> str:[m
 [m
 [m
 def _has_valid_cookie(request: Request) -> bool:[m
[32m+[m[32m    # No owner secret configured means no one can log in, so no cookie may[m
[32m+[m[32m    # ever count as valid - the HMAC key would be sha256(b""), which anyone[m
[32m+[m[32m    # can compute, making forged cookies trivial.[m
[32m+[m[32m    if not owner_secret():[m
[32m+[m[32m        return False[m
     cookie = request.cookies.get(SESSION_COOKIE)[m
     return bool(cookie and _verify_cookie(cookie))[m
 [m
[36m@@ -291,6 +296,15 @@[m [masync def oauth_authorize(request: Request):[m
     context = await _authorize_context(request, request.query_params)[m
     if context is None:[m
         return _reject("Invalid or unregistered authorization request.")[m
[32m+[m[32m    if not owner_secret():[m
[32m+[m[32m        return HTMLResponse([m
[32m+[m[32m            ERROR_HTML.format([m
[32m+[m[32m                message="No owner secret is configured; set "[m
[32m+[m[32m                "INVINCIBLE_OWNER_SECRET and restart. Authorization is "[m
[32m+[m[32m                "disabled until then."[m
[32m+[m[32m            ),[m
[32m+[m[32m            status_code=503,[m
[32m+[m[32m        )[m
     if not _has_valid_cookie(request):[m
         return _login_page(context)[m
 [m
[1mdiff --git a/tests/test_mcp_endpoint.py b/tests/test_mcp_endpoint.py[m
[1mindex 08291ef..cbf8c01 100644[m
[1m--- a/tests/test_mcp_endpoint.py[m
[1m+++ b/tests/test_mcp_endpoint.py[m
[36m@@ -230,7 +230,10 @@[m [masync def test_mcp_unknown_tool(client, bearer_headers):[m
     assert body["error"]["code"] == -32601[m
 [m
 [m
[31m-async def test_mcp_call_read_file_success(client, bearer_headers, tmp_path):[m
[32m+[m[32masync def test_mcp_call_read_file_success([m
[32m+[m[32m    client, bearer_headers, tmp_path, monkeypatch[m
[32m+[m[32m):[m
[32m+[m[32m    monkeypatch.setenv("INVINCIBLE_READ_ROOTS", str(tmp_path))[m
     target = tmp_path / "readable.txt"[m
     target.write_text("hello from disk")[m
 [m
[1mdiff --git a/tests/test_oauth.py b/tests/test_oauth.py[m
[1mindex a773008..0e9035c 100644[m
[1m--- a/tests/test_oauth.py[m
[1m+++ b/tests/test_oauth.py[m
[36m@@ -378,3 +378,49 @@[m [masync def test_mcp_with_garbage_bearer_returns_401(client):[m
     )[m
     assert response.status_code == 401[m
     assert "oauth-protected-resource" in response.headers.get("www-authenticate", "")[m
[32m+[m
[32m+[m
[32m+[m[32m# --- owner-secret-unset hardening ---[m
[32m+[m
[32m+[m
[32m+[m[32masync def test_authorize_get_rejected_when_owner_secret_unset(client, monkeypatch):[m
[32m+[m[32m    monkeypatch.delenv("INVINCIBLE_OWNER_SECRET", raising=False)[m
[32m+[m[32m    monkeypatch.delenv("MCP_SHARED_SECRET", raising=False)[m
[32m+[m[32m    verifier, challenge = pkce_pair()[m
[32m+[m[32m    client_id, redirect_uri = await oauth_register(client)[m
[32m+[m[32m    params = authorize_params(client_id, challenge, redirect_uri)[m
[32m+[m[32m    response = await client.get([m
[32m+[m[32m        f"/oauth/authorize?{'&'.join(f'{k}={v}' for k, v in params.items())}"[m
[32m+[m[32m    )[m
[32m+[m[32m    assert response.status_code == 503[m
[32m+[m[32m    assert "No owner secret is configured" in response.text[m
[32m+[m
[32m+[m
[32m+[m[32masync def test_forged_cookie_rejected_when_owner_secret_unset(client, monkeypatch):[m
[32m+[m[32m    """With no owner secret configured the cookie HMAC key would be[m
[32m+[m[32m    sha256(b"") - publicly computable. A cookie forged with exactly that key[m
[32m+[m[32m    must not authorize anything."""[m
[32m+[m[32m    import hashlib[m
[32m+[m[32m    import hmac[m
[32m+[m[32m    import time[m
[32m+[m
[32m+[m[32m    monkeypatch.delenv("INVINCIBLE_OWNER_SECRET", raising=False)[m
[32m+[m[32m    monkeypatch.delenv("MCP_SHARED_SECRET", raising=False)[m
[32m+[m[32m    verifier, challenge = pkce_pair()[m
[32m+[m[32m    client_id, redirect_uri = await oauth_register(client)[m
[32m+[m[32m    params = authorize_params(client_id, challenge, redirect_uri)[m
[32m+[m
[32m+[m[32m    payload = str(int(time.time()))[m
[32m+[m[32m    forged_key = hashlib.sha256(b"").digest()[m
[32m+[m[32m    signature = hmac.new([m
[32m+[m[32m        forged_key, payload.encode("ascii"), hashlib.sha256[m
[32m+[m[32m    ).hexdigest()[m
[32m+[m[32m    client.cookies.set("invincible_owner", f"{payload}.{signature}")[m
[32m+[m
[32m+[m[32m    response = await client.get([m
[32m+[m[32m        f"/oauth/authorize?{'&'.join(f'{k}={v}' for k, v in params.items())}"[m
[32m+[m[32m        "&action=approve",[m
[32m+[m[32m        follow_redirects=False,[m
[32m+[m[32m    )[m
[32m+[m[32m    assert response.status_code == 503[m
[32m+[m[32m    assert "location" not in response.headers  # no code is ever issued[m
[1mdiff --git a/tests/test_tool_executor.py b/tests/test_tool_executor.py[m
[1mindex f5f4ab4..70e1d3c 100644[m
[1m--- a/tests/test_tool_executor.py[m
[1m+++ b/tests/test_tool_executor.py[m
[36m@@ -281,7 +281,8 @@[m [mdef test_read_denylist_allows_source_and_config(relative_path):[m
     tool_executor.check_read_denylist(target)  # should not raise[m
 [m
 [m
[31m-async def test_read_file_returns_content(tmp_path):[m
[32m+[m[32masync def test_read_file_returns_content(tmp_path, monkeypatch):[m
[32m+[m[32m    monkeypatch.setenv("INVINCIBLE_READ_ROOTS", str(tmp_path))[m
     target = tmp_path / "hello.txt"[m
     target.write_text("hello world")[m
 [m
[36m@@ -291,7 +292,8 @@[m [masync def test_read_file_returns_content(tmp_path):[m
     assert result["content"] == "hello world"[m
 [m
 [m
[31m-async def test_read_file_missing_returns_error(tmp_path):[m
[32m+[m[32masync def test_read_file_missing_returns_error(tmp_path, monkeypatch):[m
[32m+[m[32m    monkeypatch.setenv("INVINCIBLE_READ_ROOTS", str(tmp_path))[m
     target = tmp_path / "does_not_exist.txt"[m
 [m
     result = await tool_executor.read_file(str(target))[m
[36m@@ -321,3 +323,55 @@[m [masync def test_write_file_to_protected_path_never_issues_token():[m
         tool_executor.write_file(target, "GATEWAY_API_KEY=stolen", store)[m
 [m
     assert len(store) == 0  # never staged, never approved, never written[m
[32m+[m
[32m+[m
[32m+[m[32m# --- read_file root sandbox ---[m
[32m+[m
[32m+[m
[32m+[m[32mdef test_read_outside_allowed_roots_blocked(tmp_path, monkeypatch):[m
[32m+[m[32m    monkeypatch.delenv("INVINCIBLE_READ_ROOTS", raising=False)[m
[32m+[m[32m    # tmp_path is neither the repo root nor the process CWD, so it is[m
[32m+[m[32m    # outside every allowed root and must be rejected before any disk read.[m
[32m+[m[32m    with pytest.raises(tool_executor.ToolBlocked):[m
[32m+[m[32m        tool_executor.check_read_denylist(str(tmp_path / "notes.txt"))[m
[32m+[m
[32m+[m
[32m+[m[32mdef test_read_inside_process_cwd_allowed(monkeypatch):[m
[32m+[m[32m    monkeypatch.delenv("INVINCIBLE_READ_ROOTS", raising=False)[m
[32m+[m[32m    target = os.path.join(os.getcwd(), "scratch-read-test.txt")[m
[32m+[m[32m    tool_executor.check_read_denylist(target)  # should not raise[m
[32m+[m
[32m+[m
[32m+[m[32mdef test_read_extra_root_allowed_via_env(tmp_path, monkeypatch):[m
[32m+[m[32m    monkeypatch.setenv("INVINCIBLE_READ_ROOTS", str(tmp_path))[m
[32m+[m[32m    tool_executor.check_read_denylist(str(tmp_path / "notes.txt"))  # no raise[m
[32m+[m
[32m+[m
[32m+[m[32mdef test_read_env_file_blocked_anywhere(tmp_path, monkeypatch):[m
[32m+[m[32m    monkeypatch.setenv("INVINCIBLE_READ_ROOTS", str(tmp_path))[m
[32m+[m[32m    target = tmp_path / ".env"[m
[32m+[m[32m    target.write_text("SECRET=hunter2")[m
[32m+[m[32m    with pytest.raises(tool_executor.ToolBlocked):[m
[32m+[m[32m        tool_executor.check_read_denylist(str(target))[m
[32m+[m
[32m+[m
[32m+[m[32mdef test_read_git_dir_blocked_anywhere(tmp_path, monkeypatch):[m
[32m+[m[32m    monkeypatch.setenv("INVINCIBLE_READ_ROOTS", str(tmp_path))[m
[32m+[m[32m    target = tmp_path / ".git" / "config"[m
[32m+[m[32m    with pytest.raises(tool_executor.ToolBlocked):[m
[32m+[m[32m        tool_executor.check_read_denylist(str(target))[m
[32m+[m
[32m+[m
[32m+[m[32mdef test_read_sessions_db_blocked_anywhere(tmp_path, monkeypatch):[m
[32m+[m[32m    monkeypatch.setenv("INVINCIBLE_READ_ROOTS", str(tmp_path))[m
[32m+[m[32m    with pytest.raises(tool_executor.ToolBlocked):[m
[32m+[m[32m        tool_executor.check_read_denylist(str(tmp_path / "sessions.db"))[m
[32m+[m
[32m+[m
[32m+[m[32masync def test_read_file_outside_roots_never_touches_disk(tmp_path, monkeypatch):[m
[32m+[m[32m    monkeypatch.delenv("INVINCIBLE_READ_ROOTS", raising=False)[m
[32m+[m[32m    target = tmp_path / "secret.txt"[m
[32m+[m[32m    target.write_text("do not read")[